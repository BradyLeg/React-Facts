export default function Navbar() {
    return (
        <header>
            <nav>
                <img src='../assets/react.svg' width={40}></img>
                <span>ReactFacts</span>
            </nav>
        </header>
    )
}
